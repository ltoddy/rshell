A repl for rust.

```shell
$ cargo run
In: use std::collections::HashMap;
In: let mut map: HashMap<&str, &str> = HashMap::new();
In: map.insert("hello", "world");
In: map
Out: {"hello": "world"}

In: 
```
